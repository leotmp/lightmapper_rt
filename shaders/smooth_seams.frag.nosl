
Data :: struct
{
    tex: texture_id,
    sampler: sampler_id,
}

main :: (data: ^Data @data, to_sample: vec2 @io(0)) -> vec4 @io(0)
{
    return texture_sample(data.tex, data.sampler, to_sample) * vec4(1, 1, 1, 0.5);
}
